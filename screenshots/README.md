# Screenshots

Drop terminal screenshots here to make the project easier to skim for reviewers who won't run it themselves. Suggested ones to capture:

- `main.py` running an interactive, streaming chat turn.
- `scripts/test_pipeline.py` output (characters/chunks/embedding dimensions) against a sample PDF.
- `scripts/test_rag.py` answering a question that's actually in the document, with the `Sources: Chunk N` line visible.
- `scripts/test_rag.py` correctly refusing an out-of-scope question (the "I couldn't find that information..." fallback).

Reference them from the main `README.md` once added, e.g. `![RAG answer with citations](screenshots/rag_answer.png)`.
